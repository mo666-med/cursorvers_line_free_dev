# .github/workflows/sync-line-cards.yml
# Obsidian VaultからLINEカードを同期するワークフロー
# 毎日 06:00 JST (21:00 UTC) に実行（配信の1時間前）

name: Sync Line Cards from Obsidian

on:
  schedule:
    # 毎日 JST 06:00 (UTC 21:00 前日) に同期
    - cron: '0 21 * * *'
  workflow_dispatch:
    # 手動実行も可能

jobs:
  sync-cards:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout Obsidian Vault
        uses: actions/checkout@v4
        with:
          repository: mo666-med/obsidian-pro-kit-for-market-vault
          token: ${{ secrets.OBSIDIAN_VAULT_TOKEN }}
          path: vault

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v2.x  # v1.x から v2.x に変更（ロックファイルバージョン5に対応）

      - name: Sync cards from Obsidian Vault
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          VAULT_PATH: ./vault
        run: |
          echo "📚 Obsidian VaultからLINEカードを同期中..."
          
          cd scripts/export-line-cards
          # ロックファイルの整合性問題を回避するため --reload フラグを追加
          deno run --allow-read --allow-env --allow-net --reload src/main.ts
          
          echo "✅ 同期完了"

      - name: Notify on failure
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_ADMIN_WEBHOOK_URL }}
        run: |
          if [ -n "$DISCORD_WEBHOOK_URL" ]; then
            curl -X POST "$DISCORD_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d '{"content": "⚠️ **LINEカード同期ワークフローが失敗しました**\nhttps://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
          fi
