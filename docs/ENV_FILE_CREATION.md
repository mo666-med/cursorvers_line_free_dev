# .envファイルの作成方法

## 📝 .envファイルの作成

### 方法1: 手動で作成

プロジェクトルートに`.env`ファイルを作成：

```bash
cd /Users/masayuki/Dev/cursorvers_line_free_dev
nano .env
```

以下の内容を記述：

```bash
OPENAI_API_KEY=sk-...
```

### 方法2: コマンドで作成

```bash
cd /Users/masayuki/Dev/cursorvers_line_free_dev
echo 'OPENAI_API_KEY=sk-...' > .env
```

## 📋 .env.exampleファイル

`.env.example`ファイルが作成されています。これを参考に`.env`ファイルを作成してください。

```bash
# .env.exampleをコピーして.envを作成
cp .env.example .env

# .envファイルを編集してAPIキーを設定
nano .env
```

## ✅ 確認方法

`.env`ファイルが作成されているか確認：

```bash
cat .env
```

または

```bash
ls -la .env
```

## 🔒 セキュリティ

- ✅ `.env`ファイルは`.gitignore`に追加済みです
- ✅ `.env`ファイルはGitにコミットされません
- ✅ `.env.example`ファイルはGitにコミットされます（テンプレートとして）

## 📝 使用例

```bash
# 1. .envファイルを作成
echo 'OPENAI_API_KEY=sk-...' > .env

# 2. スクリプトを実行（自動的に.envから読み込まれます）
./scripts/miyabi-chat.sh
```

## 🔗 参考

- **テンプレート**: `.env.example`
- **スクリプト**: `scripts/miyabi-chat.sh`
- **ドキュメント**: `docs/ENV_VAR_SETUP.md`

