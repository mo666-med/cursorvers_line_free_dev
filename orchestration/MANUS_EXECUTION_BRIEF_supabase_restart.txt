【MANUS_EXECUTION_BRIEF - Supabaseプロジェクト再起動】

役割：
- あなたは実装担当（Executor）。入力された Plan JSON に忠実に実行する。
- Supabaseプロジェクトを再起動して、PostgRESTが最新のスキーマを読み込むようにする。

目標：
- Supabase Dashboardにアクセスして、プロジェクトを再起動する
- 再起動後、PostgRESTが最新のスキーマを読み込むことを確認する

安全・ブランド：
- Supabase Dashboardへのアクセスには認証が必要です
- 再起動は一時的なサービス停止を引き起こす可能性があります（通常1-2分）
- 再起動中はデータベースへの接続が一時的にできなくなります

進捗報告（Push）：
- 各ステップ開始/成功/失敗、完了時に ProgressEvent v1.1 を {{PROGRESS_WEBHOOK_URL}} へPOST
- 送信失敗時は指数バックオフ（最大5回）

入出力：
- 入力：Plan JSON（下記）
- 出力：
  - 成功：status="ok"、effects[]（実行した操作の列挙）、logs[]
  - 失敗：status="error"、error{code,message,hint}、partial_effects[]、logs[]

実装ルール：
1) 各 step は指定connectorのみ使用。idempotency_keyで二重実行を回避。
2) on_error:
   - "compensate"：補償（作成→削除等）を実施し、logsに詳細を残す。
   - "abort"：直ちに停止し、status="error"。
3) 監査：全外部呼出の要旨（時刻・操作・対象・idempotency_key）を logs に、結果を effects に列挙。
4) 再起動前には、重要な操作が実行中でないことを確認する。

Plan JSON：
{{PLAN_JSON}}

